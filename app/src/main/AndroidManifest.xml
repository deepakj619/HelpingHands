<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="helpinghand.com.helpinghands">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPasswordActivity" />
        <activity
            android:name=".OrphanProfileActivity"
            android:label="@string/title_activity_orphan_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".RegisterSelect" />
        <activity android:name=".RegisterOrphanage" />
        <activity
            android:name=".DonorProfileActivity"
            android:label="@string/title_activity_donor_profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>