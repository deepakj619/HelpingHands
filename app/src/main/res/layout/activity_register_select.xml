<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="helpinghand.com.helpinghands.RegisterSelect"
    android:background="@android:color/background_light">

    <Button
        android:text="Register as Orphanage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_reg_orphan"
        android:textSize="24sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="40dp"
        android:background="@android:color/darker_gray" />

    <Button
        android:text="Register as donor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_reg_donor"
        android:textSize="24sp"
        android:layout_marginBottom="28dp"
        android:layout_above="@+id/btn_reg_orphan"
        android:layout_alignParentStart="true"
        android:background="@android:color/darker_gray" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/amilogo"
        android:id="@+id/imageView2"
        android:layout_above="@+id/btn_reg_donor"
        android:layout_marginBottom="27dp" />

</RelativeLayout>